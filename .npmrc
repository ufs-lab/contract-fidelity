# A public project resolves from the public registry. Without this, a developer
# whose machine points npm at a private mirror cannot install or lint this repo
# once their token expires, and the failure looks like a clean run: `npx` exits
# non-zero, prints an auth error, and a caller reading only the tool's own
# output sees nothing.
registry=https://registry.npmjs.org/
